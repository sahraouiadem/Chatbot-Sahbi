services:
  frontend:
    container_name: gastro-pacte-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_GEMINI_API_KEY: AIzaSyDsGYVOWMKheSErzmxh9Zfodn27d0E3TEY
    ports:
      - "3000:80"
    restart: unless-stopped
